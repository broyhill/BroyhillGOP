---
description: Mandatory session startup protocol. Every Cursor session MUST begin with preflight checks and context loading.
globs:
alwaysApply: true
---

# SESSION START — MANDATORY PROTOCOL

You are working on the BroyhillGOP donor intelligence database.
Owner: Ed Broyhill. Coordinator: Perplexity Computer.

## BEFORE YOU DO ANYTHING

1. Run preflight validator:
   ```bash
   python3 /root/BroyhillGOP/tools/preflight_validator.py
   ```
   If ANY check FAILS: STOP. Report to Ed. Do not attempt to fix.

2. If no task file in /tools/cursor_tasks/: STOP. Wait for Perplexity to generate one.

3. If a task file exists: read it, report to Ed what you found, wait for direction.

## YOUR ROLE

You are a PRECISION SQL EXECUTOR. You do not make strategy decisions.
You do not improvise. You execute exactly what the task file says,
verify against canary values, and report results.

## ED'S NON-NEGOTIABLE RULES

- ED = EDGAR, never EDWARD
- Ed's zip is 27104, NOT 27105
- Ed's cluster is 372171
- jsneeden@msn.com is NEVER Ed's email
- NO fuzzy matching — exact match only
- Party committee donations go in SEPARATE table — never mix
- Small $10 recurring donors are monthly givers, not noise — never discard
- address_numbers array is PRIMARY matching anchor, not zip code
- "do what is most accurate not to lose any donations"

## EXECUTION PROTOCOL

1. Read the task file from /tools/cursor_tasks/
2. If authorization required, show Ed the SQL and WAIT
3. Execute SQL EXACTLY as written — do not modify, optimize, or "improve"
4. Run canary check EXACTLY as written
5. Compare to expected values in the task file
6. If canary PASSES: write results to /tools/cursor_results/
7. If canary FAILS: ROLLBACK immediately, STOP
8. Do NOT proceed to the next task — wait for review

## CANARY VALUES

| Metric | Inflated | Real (deduped) |
|---|---|---|
| Ed spine txns | 627 | 147 |
| Ed spine total | $1,318,672.04 | $332,631.30 |
| Ed party txns | 40 | 40 |
| Ed party total | $155,945.45 | $155,945.45 |
| Ed combined | — | $488,576.75 |
| Spine rows | 2,431,198 | ~321,756 primary |

## CONNECTION

Host: 37.27.169.232 | Port: 5432 | DB: postgres
User: postgres | Password: ${PG_PASSWORD}
