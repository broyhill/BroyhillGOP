# Hetzner Unlock Request — Server AX162-R 2973063 / 37.27.169.232

Abuse ID: 2604:G1218ROETADB
Customer: Mr Ed Broyhill / BroyhillGOP — K1268625125
Submitted via: Reply to abuse-network@hetzner.com (subject line preserved)
Date: 2026-04-18

---

## Subject
Unlock request — AX162-R 2973063 (37.27.169.232) — incident already fully contained

## Message

Dear Hetzner Abuse Team,

Thank you for the notification. The outbound scanning event you detected
(source port 48553 → destination port 8317, beginning 2026-04-17 01:26 UTC)
was caused by an unauthorized actor who gained root access to this server by
exploiting a credential I accidentally exposed in a public repository. I
detected and fully contained the incident on 2026-04-17 between 02:00 and
06:00 UTC — the same day your detection fired — and your lock notice appears
to have been generated from that event.

Below is the complete remediation I performed, with evidence preserved on
the server at /root/incident-2026-04-17/.

### Root cause
A plaintext password for the `root` SSH account (and Postgres `postgres`
user, which shared the same password) was published to a GitHub document
on 2026-04-13. An attacker discovered it within 24 hours, logged in over SSH
from 82.162.123.255, and used the server to run `masscan` against a large
address range on destination port 8317.

### Actions taken (all completed 2026-04-17)

1. **Killed the scanner.** `pkill -9 masscan zmap nmap`. No scanning process
   has run since 2026-04-17 01:55 UTC. Verified with `ss -tnp` / `ps auxf`.

2. **Removed masscan.** `apt-get purge --autoremove masscan`. The binary is
   no longer installed.

3. **Rotated all credentials.** Root SSH password and Postgres `postgres`
   password both rotated to a fresh 24-character random string. All service
   `.env` files and backup scripts updated to match. The leaked password
   is no longer valid anywhere.

4. **Scrubbed the leaked password from the public GitHub document** and
   force-pushed. Rotated the Perplexity/internal docs as well.

5. **Removed persistence stubs.** Found and removed two malicious systemd
   units (`/etc/systemd/system/systemd.service` and
   `/etc/systemd/system/observed.service`) plus their zero-byte payload
   files in `/usr/local/bin/` (`systemd`, `free_proc.sh`, `.config.json`),
   all of which had been set immutable via `chattr +i`. Evidence preserved
   at `/root/incident-2026-04-17/persistence/`. `systemctl list-units
   --state=failed,activating` is clean.

6. **SSH hardened.**
   - `PermitRootLogin prohibit-password`
   - `PasswordAuthentication no` (key-only)
   - Banner and MaxAuthTries tightened
   - `fail2ban` installed and enabled (sshd jail active; `fail2ban-client
     status sshd` confirms).

7. **Firewall closed.** `ufw` now denies all inbound except a short
   allowlist (my home IP + Vercel/Google/Perplexity sandbox ranges for
   Postgres 5432 and admin 4399). `ufw deny out 8317` permanently blocks
   the scan port.

8. **Outbound egress.** No outbound scanning/probe traffic has been
   generated by this server since containment. You can confirm via netflow
   — the only egress is Postgres replies to the allowlisted IPs,
   rclone→Dropbox for nightly `pg_dump` backups, and standard apt updates.

9. **Evidence preserved.** Full SSH journal (30 days), `last -F`, `ps auxf`
   snapshot at containment, network state snapshot, and auth logs are all
   stored under `/root/incident-2026-04-17/`. Happy to share any of this
   on request.

10. **Integrity check passed.** All production tables and row counts are
    intact. No attacker writes, no data loss. No additional compromised
    hosts, and no sign the attacker pivoted anywhere else.

### Verification you can run
- `ps auxf | grep -Ei 'masscan|zmap|nmap'` → no hits
- `which masscan` → not installed
- `systemctl list-unit-files | grep -E '^(systemd|observed)\.service'` → gone
- `ufw status verbose` → deny-by-default with explicit allowlist
- `fail2ban-client status sshd` → active, jail running

### My commitment going forward
- Credentials are stored in a password manager only; never again in a file
  that could be committed to git. Pre-commit secret scanning
  (`gitleaks`/`trufflehog`) is being added this week.
- Monthly rotation of server credentials.
- Weekly audit of authorized_keys, systemd units, and SUID binaries via
  cron job, with diffs mailed to me.

Please unlock the IP when convenient. I am happy to answer any additional
questions or provide any of the preserved evidence files. I apologize for
the strain this put on your network.

Thank you,

Mr Ed Broyhill
BroyhillGOP
Customer No. K1268625125
ed.broyhill@gmail.com
