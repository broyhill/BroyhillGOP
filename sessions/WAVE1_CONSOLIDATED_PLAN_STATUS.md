# Wave 1 consolidated plan — status (2026-04-08)

**Plan source:** User-provided consolidated plan (GOLD NCBOE first, FEC later; voter + DataTrust foundation; `ncid` + `rnc_regid`; no source file format changes).  
**Evidence:** Repo migrations `088`–`095`, `scripts/wave1_*.sh`, live DB `isbgjpnbocdkeslofota` (spot queries).


| Phase      | Theme                                                                           | Status                            | Notes / evidence                                                                                                                                                                                                                                                                           |
| ---------- | ------------------------------------------------------------------------------- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **0**      | Lock scope, inputs, success criteria; short internal spec                       | **Partial**                       | Plan exists in chat + this file; **no** separate signed-off spec in repo for recipient filter, contributor rule, key hierarchy, “registered voter” definition beyond what migrations/views encode.                                                                                         |
| **1A**     | Snapshots; confirm row counts / join paths                                      | **Partial**                       | `pipeline.wave1_source_snapshots` exists (`088`); `**0` rows** — import dates not being recorded in practice. `ref.v_nc_voter_canonical`, `ref.v_datatrust_registration` document join paths (`ncid`, `statevoterid`, `rnc_regid`).                                                        |
| **1B**     | Canonical registration (NCSBE wins; DataTrust A/I mapped)                       | **Largely done (SQL)**            | `088`: `ref.v_nc_voter_canonical` (`A/I/R/D` → buckets), `ref.v_datatrust_registration` (A/I + unknown). **Operational:** analysts must use views, not ad hoc mixed predicates.                                                                                                            |
| **1C**     | Conformed birth_year / age typing                                               | **Unknown / partial**             | Not fully audited here; `088` focuses on voter/DataTrust **registration** views and staging **bigint** fixes, not a full conformed demographic layer on `person_spine`.                                                                                                                    |
| **1D**     | `is_registered_voter` = explicit definition; recompute from `nc_voters`         | **Done (migration)**              | `089_WAVE1_REFRESH_SPINE_VOTER_FLAGS.sql`: TRUE only when `nc_voters.status_cd = 'A'`; clears flag when no voter row. **Verify:** re-run after major voter file reloads.                                                                                                                   |
| **1E**     | `donor_voter_links`: quarantine orphans; re-match + `match_method` / confidence | **Partial**                       | `090`: `is_orphaned` tagging + `091` trusted view + `092` index — **non-destructive**. **DB (spot check):** ~~**114k** orphaned of ~**221k** total (~~52%) — **not** near zero; full re-match pipeline **not** done.                                                                       |
| **1F**     | DataTrust “full consumer” via jsonb / split tables                              | **Not done** (by design deferral) | `nc_datatrust` remains wide table in DB; plan said don’t add 2,200 scalars — **no** jsonb chunk layer verified as the ingestion path for a new full extract.                                                                                                                               |
| **1 exit** | Status consistent, links trustworthy, typed layer for age agreed                | **Not met**                       | `1E` orphan rate blocks “trustworthy links”; `1C` / `1F` incomplete.                                                                                                                                                                                                                       |
| **2**      | GOLD → raw → norm; idempotent loads; zero/negative rules                        | **Not complete**                  | `pipeline.loaded_ncboe_files`: **7** files (not the 18 GOLD set by filename). `nc_boe_donations_raw`: **416,297** rows; `**dedup_key` populated: 0**. `norm.nc_boe_donations`: **581,741** rows (historical / other path). **Row-count reconciliation to GOLD file totals:** not verified. |
| **3**      | Match → dedupe → rollup → spine; `spine_clusters` bigint; spine GOP slice QA    | **Partial infra**                 | `088` upgrades `staging.spine_clusters` / `spine_merge_candidates` to **BIGINT** when tables exist. **Exit criteria** (norm vs spine reconciliation, high-dollar audits, reported match rates): **not** verified.                                                                          |
| **4**      | FEC separate; maps populated                                                    | **Out of Wave 1**                 | Per plan — still secondary; not scored here.                                                                                                                                                                                                                                               |
| **5**      | Analyst views, QA dashboard, retention                                          | **Partial**                       | Ref views (`088`, `093`, `094`, etc.) exist. **QA dashboard** / retention policy: **not** seen as a delivered artifact.                                                                                                                                                                    |


## “What you should not do” (Wave 1)


| Rule                                                                       | Status                                                                                                                                                                                                                                                          |
| -------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Don’t blend FEC into GOLD individual spine build                           | **Process-dependent** — repo keeps separate scripts/tables; **ops discipline** required.                                                                                                                                                                        |
| Don’t widen Postgres with thousands of DataTrust scalars                   | **OK so far**; `1F` still open if new full file arrives.                                                                                                                                                                                                        |
| Don’t trust `donor_voter_links` / `is_registered_voter` until Phase 1 exit | `**is_registered_voter`:** migration addresses definition **if** `089` applied and voter file current. `**donor_voter_links`:** still **do not trust** for production reporting without filtering (`ref.v_donor_voter_links_trusted` or `is_orphaned = false`). |


## Spine columns (spot check)

`core.person_spine` includes `**voter_rncid`** and `**rnc_regid**` (in addition to voter_ncid-style fields) — aligns with “both keys when available” **at schema level**; population rates not audited here.

## Recommended next moves (to advance the plan)

1. **Phase 0:** Add a one-page `docs/WAVE1_SCOPE_SPEC.md` (recipient filter, individual-only, key hierarchy, registered-voter definition) — bullet list is enough.
2. **Phase 1A:** After each `nc_voters` / `nc_datatrust` / GOLD load, `INSERT` into `pipeline.wave1_source_snapshots`.
3. **Phase 1E:** Run orphan remediation / re-match job (or SQL) to cut orphan rate; add `match_method` / `confidence` if not present.
4. **Phase 2:** Decide manifest **Option A/B/C** (`sessions/NCBOE_FILE_MANIFEST.md`); run `scripts/wave1_ingest_gold_ncboe.sh` against Desktop GOLD; normalize; confirm **18** files in `loaded_ncboe_files` and `dedup_key` > 0 on the path you use.
5. **Phase 3:** After Phase 1 exit + Phase 2, run spine merge with bigint-safe staging and publish match-rate / rollup QA queries.

